// Technical Analysis tab — placeholder shell (Session 4, Commit 1).
//
// Intentionally empty: the Header Strip, filter bar, and the six
// aggregation sections (MACD state grid, VWAP/EMA distance, combined
// signal reads, time-of-day) land in Commits 2-6. This honestly-labeled
// placeholder keeps a mid-session packaged build from looking broken
// when the Technicals tab is clicked.

export default function TechnicalsTab() {
  return (
    <div className="space-y-6">
      <div className="rounded-lg border border-border-subtle bg-bg-2 p-6 text-center text-sm text-fg-secondary">
        Technical Analysis tab — in development.
      </div>
    </div>
  )
}
