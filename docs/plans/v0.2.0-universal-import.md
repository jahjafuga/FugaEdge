\# v0.2.0 — Universal Import



\*\*Status:\*\* Planning complete, ready to execute

\*\*Target ship:\*\* 10-14 working days from start

\*\*Started:\*\* \[tomorrow's date]

\*\*Theme:\*\* "FugaEdge works regardless of broker"



\---



\## Why this release exists



Within 24 hours of v0.1.6 going public on GitHub, three real momentum traders found FugaEdge and tried it. All three hit walls at the import step:



\- Tester A (DTSM, DAS Trader) — tried execution-level CSV, blocked

\- Tester B (DAS Trader) — same execution CSV, blocked

\- Webull user (DTSM connection unknown) — tried both Webull mobile CSV and desktop XLSX, both blocked



v0.1.6 only parsed one CSV format: the DAS Trader summary export, which is what Lao personally uses. Most experienced DAS users export execution-level data instead. Webull users export entirely different formats.



v0.2.0 fixes this by introducing a universal import architecture: one internal data model, multiple parsers per broker/format, auto-detection on import.



This is the release that unblocks the DTSM beta cohort and opens FugaEdge to the Webull user base.



\---



\## What ships



\### Parser layer

\- DAS Trader Summary CSV (existing, refactored to universal model)

\- DAS Trader Execution CSV (NEW)

\- DAS Trader Account Report CSV (NEW, for fee data)

\- Webull Mobile/Web CSV (NEW)

\- Webull Desktop XLSX (NEW)



\### Auto-detection

\- Sniff the first row of any uploaded file

\- Match against known signatures

\- Route to correct parser

\- Smart error messages when format unknown

\- "Detected: DAS Execution CSV" feedback during import



\### Enrichment pipeline

\- Float auto-fetch via Polygon on ALL import paths (currently only fires on summary path)

\- Country auto-detection via Polygon ticker details

\- Async processing with progress UI

\- Manual override always available

\- Graceful failure handling



\### Fee handling

\- Multi-file dual-import: executions + Account Report

\- Match by date + symbol

\- Allocate fees proportionally for multi-roundtrip days

\- Support negative fees (ECN rebates — confirmed in Tester B's data)

\- Honest UI: "Fees: not reported" instead of misleading $0.00



\### Bug fixes

\- Reports Daily P\&L aggregator undercount on multi-roundtrip days

\- Light/dark mode token contamination on Trades cards

\- All hardcoded colors replaced with CSS variables



\### UX improvements

\- In-app broker export guide with screenshots

\- Better error messages everywhere

\- Format detection feedback during import



\---



\## What does NOT ship in v0.2.0



Deferred to v0.2.1 or later:

\- Lightspeed CSV parser

\- IBKR Flex Query XML parser

\- ThinkOrSwim CSV parser

\- TradeStation CSV parser

\- Tradervue generic CSV parser

\- Trade deletion (single trade, bulk by date range)

\- Undo import by import session ID

\- Restore-from-backup UI (browse backups, select, confirm, restart with schema-version safety on older backups)

\- 30-day soft delete with recovery



Deferred to v0.3.0:

\- shadcn UI migration

\- 21st.dev component blocks

\- Timezone architecture

\- Currency conversion system

\- Language / i18n system

\- Locale-aware date and number formatting

\- Mac developer preview



Deferred to v0.4.0+:

\- iPhone companion app

\- Pro tier launch

\- Cloud sync

\- Auto-catalyst detection from Polygon News API

\- Video Analyst feature

\- Multi-account support

\- Lightspeed, IBKR, ToS, TradeStation parsers



\*\*Discipline rule:\*\* if a v0.2.0 conversation surfaces something cool that's not in the scope above, write it in `docs/plans/v0.3.0-or-later-ideas.md` and do not build it in v0.2.0.



\---



\## Architecture



\### Universal data model



Two internal types, broker-agnostic:



\*\*Execution\*\* (one fill):

\- id, source\_broker, source\_format, source\_file

\- timestamp\_utc, symbol, side, quantity, price

\- order\_id (for grouping partial fills)

\- route, liquidity\_type

\- commission, ecn\_fee, sec\_fee, finra\_fee, cat\_fee, htb\_fee, other\_fees

\- account\_name, is\_paper



\*\*RoundTrip\*\* (aggregated from executions):

\- id, symbol, account\_name, side (LONG/SHORT)

\- entry\_time\_utc, exit\_time\_utc

\- shares, avg\_entry\_price, avg\_exit\_price

\- gross\_pnl, total\_fees, net\_pnl, fees\_reported (boolean)

\- execution\_ids (links back)

\- User fields: playbook, catalyst\_type, mistake, confidence, notes

\- Enriched: float, country, market\_cap



\### Adapter pattern



Each broker/format gets a thin parser that produces Execution\[]:



\- DAS Summary CSV → adapter → Execution\[]

\- DAS Execution CSV → adapter → Execution\[]

\- DAS Account Report CSV → adapter → fee data matched to existing executions

\- Webull Mobile CSV → adapter → Execution\[]

\- Webull Desktop XLSX → adapter → Execution\[]



After parsing, the rest of FugaEdge doesn't care about source broker.



\### Round trip builder



One function, broker-agnostic:



Takes Execution\[] sorted by timestamp.

For each symbol:

\- Tracks running position

\- Detects when position goes from 0 to non-zero (new round trip)

\- Detects when position returns to 0 (round trip ends)

\- Handles adds to position (weighted avg entry)

\- Handles short selling (Sell before Buy = short)

\- Handles partial fills via order\_id grouping

\- Handles open positions at end of file



Returns RoundTrip\[].



\### Import pipeline



1\. User drops file(s)

2\. Detect format(s) from header sniffing

3\. Parse each file → Execution\[] or fee data

4\. Merge executions, deduplicate

5\. Build round trips via universal builder

6\. Match fees if Account Report present

7\. Enrich async (float, country)

8\. Save to database

9\. Show result with progress UI



\---



\## Architecture decisions



\### Decision 1 — Date handling for execution CSVs (no date column)



DAS execution CSVs only have time, not date. Resolution:



\- If filename matches YYYY-MM-DD.csv or MM-DD-YYYY.csv → use that

\- Otherwise → show date picker, prompt user

\- If user also drops Account Report → use that file's date and skip prompt



\### Decision 2 — Multi-file import UX



User can drop one or both files at once:



\- If both detected → process combined

\- If execution only → process but show banner: "Add Account Report CSV for fee data"

\- If summary only → process as before (backwards compatible)



\### Decision 3 — Migration for existing v0.1.6 data



Full one-time migration on first v0.2.0 launch:



\- Auto-backup database before migration (rollback option)

\- Show progress banner: "Upgrading database for v0.2.0..."

\- Convert all existing trades to new universal model

\- Verify integrity with checksum

\- Tests verify nothing is lost



\---



\## Build sequence



10-12 working days. Each day is a focused Claude Code session.



\### Day 1 — Universal data model

\- Create /src/core/import/types.ts with Execution and RoundTrip

\- Refactor existing DAS Summary parser to produce Execution\[]

\- Database schema migration for new columns

\- All v0.1.6 tests still pass



\### Day 2 — DAS Execution CSV parser

\- Parse Tester B's CSV format end-to-end

\- Date handling (filename + prompt)

\- Save Tester B's CSV (sanitized) as test fixture

\- Round trip builder verified on his data



\### Day 3 — Edge cases + Account Report parser

\- Shorts, adds, open positions, multiple symbols, partial fills

\- DAS Account Report CSV parser for fee data

\- Fee matching by date + symbol

\- Proportional fee allocation for multi-roundtrip days



\### Day 4 — Webull Mobile CSV parser ✅ SHIPPED 2026-05-17

\- Parse the Webull user's mobile CSV format

\- Handle @-prefixed prices, EDT timestamps

\- Convert to UTC internally — **revised to: store bare local Eastern (no TZ conversion)** per Day 4 design decision to match the existing DAS-parser convention. v0.3.0 ticket [[store-true-utc-timestamps]] handles the cross-codebase UTC migration when it lands.

\- Tests



Shipped artifacts (Track A bundle, commit `29cfde1`):

\- `electron/import/parse-webull-mobile.ts` (261 lines) — pure parser, @-prefix stripping, title-case Buy/Sell → 'B'/'S', EDT/EST literal validated then discarded, wbm-<sha1\[0..12]> synth IDs

\- `electron/import/__tests__/parse-webull-mobile.test.ts` — 43 tests covering happy path, bad-row handling, synth ID stability, three exported helpers, real-fixture invariants



Deferred to v0.3.0:

\- Per-date DST verification on Webull Mobile timestamps (single fixture was DST-only; year-round abbreviation behavior unverified). Filed as [[per-date-dst-verification]].

\- Multi-timezone Webull users (truck driver case Lao flagged) — Track A currently skips + traces on non-EDT/EST timestamps. Acceptable while we wait for confirmation.



\### Day 5 — Webull Desktop XLSX parser ✅ SHIPPED 2026-05-17

\- Read XLSX format (via exceljs, added as dev dep)

\- Handle ISO 8601 UTC timestamps — **revised to: convert UTC → America/New\_York via Intl.DateTimeFormat, then store bare local Eastern** to match Day 4 + existing DAS-parser convention

\- ~~Detect paper trading flag~~ — **revised: no auto-detect.** User declares via Account Type toggle in import preview (Day 6 Track C). v0.3.0 ticket [[paper-account-import-and-filtering]] handles auto-detect once downstream filtering ships.

\- Tests



Shipped artifacts (Track B bundle, commit `5f4780b`):

\- `electron/import/parse-webull-desktop.ts` (304 lines) — pure parser, exceljs sheet read, strict "Order" sheet name check, required-column validation, UPPERCASE BUY/SELL → 'B'/'S' (separate normalizer from mobile per cross-format isolation rule), wbd-<sha1\[0..12]> synth IDs, midnight 24→00 defensive normalization

\- `electron/import/__tests__/parse-webull-desktop.test.ts` — 40 tests, synthetic XLSX builder pattern (no binary fixtures committed), DST transition coverage (Nov 1 2026 both sides), real-fixture invariants

\- `scripts/redact-webull-xlsx.cjs` + `scripts/audit-webull-xlsx.cjs` — fixture tooling, shipped in commit `e9d0c9b`



\### Day 6 — Format auto-detection + universal-import wiring ✅ SHIPPED 2026-05-17 (Track C, 5 steps + fix-up)

\- Sniff first line/sheet to identify format

\- Route to correct parser

\- Friendly errors for unknown formats

\- Tests for each format



Shipped artifacts (Track C, 5-step bundle + fix-up, 8 commits total):

\- `5370b76` step 1: detect-format extension — `webull_mobile` sniff branch (three-marker fingerprint: first-column Name + Filled Time + Time-in-Force); `webull_mobile` and `xlsx` added to shared CsvFormat for FileInfo

\- `7040915` step 2: IPC routing — async handler, XLSX extension routing upstream of detect-format, three specific warning strings for failure modes (renderer/IPC mismatch, parser throw, missing text)

\- `8e102e7` step 3: DropZone XLSX read — accepts `.csv` + `.xlsx`, branches on extension (`file.text()` vs `file.arrayBuffer()` → Uint8Array), copy broadened broker-agnostic, OnboardingModal caller updated

\- `b6de121` step 4: ImportSummary pill + label — `webull_mobile` and `xlsx` formats join `tradehistory`/`trades_window` in the info-blue "trades" pill branch

\- `4a23d7b` step 5: Account Type toggle — per-batch Real/Paper radio in PreviewPanel, disables Import on Paper with deferral message ("Paper imports arrive in v0.3.0")

\- `fbec771` step 5 fix-up: theme-aware token migration (7 swaps across Account Type panel, Date-required banner, Fees-not-included banner) after light-mode visual regression discovered post-step-5 sign-off



Discovery sequence worth preserving for audit trail:

1\. Step 5 shipped with dark-mode-only visual verification.

2\. User-driven light-mode test pass on 2026-05-17 revealed three distinct bugs: Account Type panel rendering as flat dark block, Fees-not-included banner text near-invisible (every Webull import path), disabled-button gold washing toward beige.

3\. Root cause: legacy hex aliases in `tailwind.config.ts:84-101` that don't theme. The Account Type panel inherited the convention from sibling components (ImportSummary uses every legacy alias) without realizing the convention itself was broken in light mode.

4\. Narrow fix-up landed: Account Type panel + Fees-not-included + Date-required banner inner text migrated to themed `*-fg-*` / `bg-bg-*` tokens. Wider migration deferred to v0.3.0.



Deferred to v0.3.0 (filed in `docs/plans/v0.3.0-or-later-ideas.md`):

\- [[paper-account-import-and-filtering]] — toggle stores correct metadata but downstream aggregation paths (dashboard, edge insights, streak, P&L) don't filter `is_paper=1` yet. v0.2.0 toggle disables Import on Paper rather than ship partially-implemented filtering.

\- [[import-flow-legacy-hex-alias-migration]] — ImportSummary.tsx (8+ usages) + stale tailwind.config.ts:84 comment cleanup. Account Type panel + adjacent banners already done; ticket scope refined in commit `b7d9635`.

\- [[disabled-button-light-mode-polish]] — `disabled:opacity-40` on `bg-gold` washes to beige on light surfaces. Pre-existing, affects all gold buttons across the app.

\- [[store-true-utc-timestamps]] — bundled with the timezone-architecture cleanup; touches all six parsers + display layer.

\- [[per-date-dst-verification]] — Webull Mobile EDT/EST literal trust-the-tag-only verification.



End-to-end smoke test (step 6 of the original 6-step Track C plan) deferred to Day 12 ship-day smoke per session-energy and Day 12's existing "Final smoke test" item that re-imports all real user fixtures.



\### Day 7 — Enrichment pipeline

\- Refactor float fetch to fire on ALL import paths

\- Add country detection from Polygon

\- Async queue with progress UI

\- Manual override available

\- Tests with mocked Polygon

- API key onboarding UX (added from Tester B testing 2026-05-16):
  - First-time install banner about Polygon API key requirement
  - "Get free API key" prominent CTA link in Settings
  - Test API key validity on save (immediate feedback: works or invalid)
  - Empty-state handling when enrichment fails:
    - Banner: "Country/float couldn't be fetched. Check API key in Settings."
    - Modal when clicking backfill without key set, with direct Polygon signup link
  - Better error messages throughout enrichment flow
  - In-app explanation: "What does this API key do?"
  - Save where user was so they can return after API key setup



\### Day 7.5 — Database safety patch

Two-piece safety net. Auto-backup is the entire recovery story in v0.2.0 since deletion / restore UI is deferred to v0.2.1. Closes the gap that bit Tester B on 2026-05-19 (wrong daily-summary CSV imported, no rollback path, manual rename-via-File-Explorer was the only recovery).

- Silent auto-backup before every import:
  - Before any import handler writes to the DB, copy fugaedge.db to %APPDATA%\fugaedge\backups\fugaedge-pre-import-<ISO-timestamp>.db
  - Applies to every parser path: DAS Summary, DAS Execution, DAS Account Report, Webull Mobile, Webull Desktop XLSX, any future format
  - Retention: keep last 20 backups, prune oldest on each new backup
  - Architecture (per ARCHITECTURE.md): file copy goes through a /src/platform/* storage abstraction so a web port can swap to server-side blob storage; trigger logic lives in /src/core or /src/lib and accepts the storage interface as a parameter
  - If backup write fails, abort the import with a clear error — do NOT import without a backup, that defeats the purpose
  - Backups folder auto-created on first import
  - Log backup path on success so DM walkthroughs reference the real file

- "Reset journal" button in Settings:
  - Single button, double-confirmation modal ("type DELETE to confirm")
  - On confirm: rename current fugaedge.db → fugaedge-reset-<ISO-timestamp>.db and restart app (fresh DB created on next launch). Same rename-not-delete pattern as auto-backup
  - No "restore from backup" UI in v0.2.0 — manual recovery only (close app, open backups folder, rename desired .db back to fugaedge.db, relaunch). Restore UI lands in v0.2.1

- Acceptance criteria:
  - Every import path produces a timestamped backup BEFORE any executions or round_trips are written to the DB
  - Backup count never exceeds 20 in the backups folder
  - Backup file is binary-identical to source (validate size + checksum)
  - Reset button rename + restart leaves a fresh empty DB and a safety-net .db file in %APPDATA%\fugaedge\
  - Second Reset click succeeds without filename collision (ISO timestamps prevent collisions in practice)

Estimated effort: 2-3 hours. Slots after Day 7 (enrichment pipeline stabilises the import-handler surface) and before Day 8 (aggregator fix + light/dark audit) so safety lands before polish.

**Piece 3 status (2026-05-20): PASSED.**

Runtime acceptance test executed against test-fixture Tester A's executions CSV import + reset against live journal. All Part A acceptance criteria (auto-backup file exists at `%APPDATA%\fugaedge\backups\fugaedge-pre-import-<ts>.db`, size matches live DB, `[FJ backup]` log fired, backup precedes import completion) and all Part B acceptance criteria (button placement, destructive styling, modal copy, type-DELETE gate case-sensitive and trim-tolerant, Cancel path no-op, Confirm triggers rename, sibling file at correct path NOT in `backups/`) verified clean.

Shipped commits: ec51878 (piece 1 auto-backup), e80d0ac (piece 2 Reset journal button).

One dev-mode finding surfaced and deferred to v0.3.0: `app.relaunch()` does not relaunch the dev pipeline in `npm run dev` mode (works correctly in packaged builds — zero end-user impact). Parked in fa18aef.



\### Day 8 — Aggregator fix + light/dark audit

- Fix Reports Daily P&L multi-roundtrip undercount
  - Note (2026-05-21 audit): bug is structurally absent in current code — the Day 1 universal-import rewrite (3f1df7d) replaced the orphaned compute-trips.ts path (ed1532a, v0.1.0) that the bug doc flagged. build-round-trips.ts:91-102 closes a round trip on each net-zero return; repo.ts:158 keys conflicts on exec_hash not (symbol, date); metrics.ts:42-65 sums per-day without grouping by symbol. The remaining work is verification, not repair: write the missing aggregation test (src/core/performance/__tests__/metrics.multi-roundtrip.test.ts), run the ODYS anchor case end-to-end against 2026-05-14-v0.1.7-multi-roundtrip-bug.md, retire the bug doc from "Draft (not implemented)" to closed-with-evidence.
  - **Day 8 scope addendum (2026-05-21):** Tester A's side-by-side chart evidence later today surfaced a 4-hour timezone-rendering bug on the trade detail chart. The fix is large enough to slot as Day 8.5 (see below) rather than absorbing into Day 8 polish work.

- Verify Cumulative P&L, Daily Volume, Win % all correct

- Audit all components for hardcoded colors
  - **Known contamination surfaces (Day 10 hunt, 2026-05-23):**
    - Trades page cards (already known)
    - Import preview trades table (Day 10 finding — table renders in dark inside light-mode parent page)
    - Suspected additional surfaces to verify during the audit: Calendar weekly grid, Reports tables, Deep Analytics tables (all 6 tabs). Pattern across all of these: a table component styled with hardcoded dark-theme colors that was never wired to theme tokens.
  - When running the sweep, grep for hardcoded color values that bypass the theme system — specifically `#000`, `#fff`, `rgb(`, `rgba(`, `bg-black`, `bg-white`, `text-white`, `text-black`. These are the patterns causing the contamination.

- Replace with CSS variables

- Test every page in both themes


- Number formatting consistency pass:
  - Float displays correctly (1.47M not 1.5M — 2 decimals in millions)
  - Float buckets: under 1M as K, 1M-99.99M as 2-decimal M, 100M+ as whole M, 1B+ in B
  - Prices always 2-4 decimal places, never round to whole
  - Share counts with commas, never abbreviated
  - P&L in currency format, 2 decimals
  - Percentages 1 decimal default
- Version display fix (sidebar shows V0.1.0, should show actual package.json version): ✅ SHIPPED 75d5aee (2026-05-21)
  - ✅ Closed (2026-05-23 audit): fixed in 75d5aee — `Sidebar.tsx:199` now reads `useAppVersion()` (`src/lib/useAppVersion.ts`) which calls `ipc.getVersion()` → `app.getVersion()` in the main process, reading from `package.json` at runtime. Sweep: no other hardcoded current-version strings in `src/`, `electron/`, `shared/`; intentional roadmap signposts ("…arrive in v0.3.0") and the `pre-v0.2.0-*.bak` migration-marker filename are correctly left static.
- Float auto-fetch wiring: verify it runs on ALL import paths (currently broken on dual-file imports — Polygon has the data, FugaEdge isn't fetching on this path): ✅ SHIPPED 7e92f70 (2026-05-17, Day 7A series closing patch)
  - ✅ Closed (2026-05-23 audit + empirical verify): the Day 7A series (`f52f9d9`, `d191d66`, `45c17ab`, `58576b7`, `5d104b7`, `3a54711`, `7e92f70`) rewrote post-commit enrichment so `enrichFloatForImportedSymbols(newSymbols)` fires off `toInsertTrips` in `electron/import/ipc.ts:483-543`, after the parser path has converged — single-file and dual-file imports take the same enrichment branch. Empirical confirmation 2026-05-23: dropping `test-fixtures/tester-b_trades_2026-04-02.csv` into the dev build produced `[FE import] float fetched=8 missing=0 errored=0`.
- Audit all UI numeric helpers for consistency
- Found during v0.1.6 live testing on 2026-05-16

- Import UI cosmetics (deferred from Day 2 smoke test 2026-05-16):
  - UNKNOWN format pill in ImportSummary should map 'tradehistory' and 'trades_window' to friendly labels (currently shows "unknown")
  - "Saving to database…" UI lacks clear completion feedback
  - PreviewTable.tsx:48 React key warning (pre-existing)

- Edge Insights — collapsible + dense list view: DEFERRED to
  v0.3.0 (see docs/plans/v0.3.0-or-later-ideas.md). Original Day
  8 detail preserved in git history.

- Edge Insights — day-name truncation bug: ✅ SHIPPED b2ebb52 (2026-05-16)
  - Card currently reads "Thus are your strongest day / Tues lose −$195" — should be "Thursday is your strongest day / Tuesdays lose −$195".
  - Day names are being truncated, likely by a `.slice(0, 4)` or similar helper on day-of-week strings. Truncation breaks subject/verb agreement in templated copy.
  - Acceptance: all 7 day names render in full across all insight rules. Sweep for similar truncation on month names, symbol names, or any other field using the same helper.
  - ✅ Closed (2026-05-21 audit): fixed in b2ebb52 — `dowName()` in `src/core/insights/rules.ts` now returns full day names ('Sunday'…'Saturday'). No remaining `.slice`-style truncation on day, month, or symbol strings.

- Weekly Review — trade list scalability: DEFERRED to v0.3.0
  (see docs/plans/v0.3.0-or-later-ideas.md). Original Day 8
  detail preserved in git history.

- Playbook screen bugs — verified 2026-05-21 (Tester A, DTSM beta cohort):

  - **"New" playbook button silently fails.** Root cause verified
    at `src/pages/Playbook.tsx:77-79`: `handleCreate` calls
    `window.prompt('New playbook name?')` to ask for the name.
    Electron's renderer does not implement `window.prompt()` —
    it always returns `null`. The guard `if (!name) return` then
    bails before any IPC call. The downstream IPC chain
    (`ipc.playbookCreate` → `IPC.PLAYBOOK_CREATE` handler →
    `createPlaybook()` → SQLite INSERT) is intact and works
    correctly once invoked.

    Fix: replace `window.prompt` with an in-app input (inline
    field or small modal) that captures the name, then call
    `ipc.playbookCreate` with the captured value. Acceptance:
    create a new playbook end-to-end, confirm it persists across
    app restart.

  - **"Archive" playbook button is a staged edit disguised as an
    action.** Root cause verified at `src/pages/Playbook.tsx:264-266`:
    Archive's onClick only mutates local React editor state via
    `setEditor({ ...editor, archived: !editor.archived })`. It
    never calls IPC. Persistence requires a separate Save click,
    which the user has no signal they need to perform. Navigating
    to another playbook and back re-seeds editor state from the
    DB-backed selected playbook, reverting the label silently.
    The schema (`electron/db/schema.ts:119`) and repo
    (`updatePlaybook`) handle archived correctly — the data layer
    is fine.

    Fix: make Archive persist immediately via
    `ipc.playbookUpdate({ id, archived })` rather than staging
    via setEditor. Acceptance: archive a playbook, close the
    app, relaunch, confirm the playbook is still archived.

  - Both fixes touch `src/pages/Playbook.tsx`. While in the file,
    check whether the create + archive logic mixes with IPC
    plumbing in a way that violates ARCHITECTURE.md rule #1.
    If so, extract pure logic to /src/core/playbook or
    /src/lib opportunistically — don't replicate the legacy
    pattern. Don't let the refactor block the fix.

- Import preview screen — sticky bottom action bar (Tester A, 2026-05-21):

  Cancel + "Import N trades" / "Nothing new to import" buttons
  currently sit below a long scrollable trades preview table.
  On laptop screens they fall below the fold, requiring the user
  to scroll past 99+ rows to find them. The buttons are central
  to the import flow and must always be visible.

  Fix: stick the action bar to the bottom of the import modal
  regardless of scroll position. Single canonical action bar,
  always visible. Do NOT duplicate buttons at the top of the
  modal — creates ambiguity and looks like a UX patch-job.
  Acceptance: on a 99-row import preview at 1080p laptop
  resolution, both Cancel and the primary action button remain
  visible without scrolling.

- Save Mistakes — completion confirmation (Tester A, 2026-05-21):

  Verified UX-only, not a data integrity bug. The save chain
  (`MistakesTab.save()` → `onSaveMistakes` → `handleSaveMistakes`
  → `ipc.tradeMistakesSave` → `IPC.TRADE_MISTAKES_SAVE` →
  `saveMistakes()` → `UPDATE trades SET mistakes_json = ?`)
  works correctly. The reported symptoms are all real but
  cosmetic:

  - Button goes disabled because the post-save trade update
    flips `dirty = !sameArray(selected, trade.mistakes)` to
    false. Correct behavior — there's nothing left to save.
  - No "saved" confirmation text appears (unlike the Playbook
    editor which shows a saved pill).
  - Modal staying open is by design — multi-tab workflows
    (Mistakes → Notes → Attachments) depend on it.

  Fix: add a transient "saved" confirmation in `MistakesTab.tsx`,
  mirroring the existing Playbook editor's `savedAt` pill
  pattern. Recommend 1.5-second visible duration. Do NOT
  auto-close the modal; do NOT redesign the disabled-state
  behavior — both are working correctly. Bundles with existing
  Day 8 item *'Saving to database…' UI lacks clear completion
  feedback* — same fix shape, same release.

- Calendar day-trades panel — visibility on click (Tester A, 2026-05-21):

  Verified the click logic works as built. Calendar cells are
  `<button type="button">` elements with working onClick
  handlers at `src/components/calendar/CalendarGrid.tsx:205-207`.
  Clicking a day with `trade_count > 0` calls `onSelectDate`,
  which triggers a `useEffect` at `src/pages/Calendar.tsx:108-123`
  to fetch trades and render `<DayTradesPanel>`. Clicking a
  zero-trade day opens `NoTradeDayModal` instead (separate
  sit-out feature).

  The bug Tester A reports ("nothing happens") has two likely causes:

  1. The user clicked a zero-trade day and saw the sit-out modal,
     not a trades list — to them this looks like "no trades."
  2. `DayTradesPanel` renders **inline at the bottom of the page**,
     below the ~660px-tall calendar grid. On a 1080p laptop
     viewport, it appears below the fold and the user doesn't
     scroll. Same root cause as the import buttons below the
     fold — FugaEdge has a recurring "important content below
     the laptop fold" pattern.

  Fix: scroll DayTradesPanel into view on open, OR render it as
  an overlay/side panel so it's visible without scrolling.
  Recommend the scroll-into-view option as the cheaper change.
  Acceptance: clicking a day with trades on a 1080p laptop
  viewport must result in the trades panel being immediately
  visible without user scrolling.

  Worth noting: this is the third item this release tracing
  back to "Tester A is on a laptop, FugaEdge renders important UI
  below the viewport fold." Consider a brief audit pass for
  other surfaces with the same problem (Edge Insights expansion,
  Reports drill-downs, Sessions detail view) while you're in the
  area.

- EMA9 label clarity (Tester A, 2026-05-21):

  Verified the indicator is computed on 1-minute OHLC bars.
  `computeEma9Distance` at `electron/market/intraday.ts:337`
  reads from the `intraday_bars` table, populated exclusively
  by `fetchIntradayMinutes` which calls
  `/v2/aggs/.../range/1/minute/...` — i.e. multiplier 1,
  timespan minute. The function locates the entry bar via
  "start ≤ entry < start + 60s", requires ≥9 bars to seed the
  EMA, and runs `ema(closes, 9)` over the close prices. The
  Trade Detail tooltip at `TradeDetailModal.tsx:420` already
  correctly states "over 1-minute bars."

  Fix: relabel the stat card from "EMA9" or "ENTRY VS EMA9" to
  "ENTRY VS 9EMA (1m)" (or whichever exact wording matches the
  rest of FugaEdge's labeling convention — check the Trades-
  table-column variant from the v0.2.1 ticket if shipped, to
  keep them consistent). Apply rename to every surface the
  label appears in — single source-of-truth string preferred


\### Day 8.5 — Timestamp rendering fix

Pull forward the timestamp work from the v0.3.0 parking lot. Surfaced by Tester A 2026-05-21 with direct evidence: side-by-side RUBI chart on FugaEdge vs Chartswatcher shows a 4-hour offset (FugaEdge plots entry at 11:00; Chartswatcher plots same trade at 07:00). The 4-hour delta matches EDT-to-UTC, confirming the chart code treats `executions.timestamp_utc` as UTC when the column stores bare local Eastern. Same root cause as the AEHL and VEEE entry-marker bugs flagged earlier — those are not pre-market-window issues, they are timezone-rendering issues.

**Two-part fix**

1. **Store true UTC timestamps in the database**
   - Add parser helper `localEasternToUtc(date: string, time: string): string` applied to all six parsers (4 DAS + 2 Webull). Output: ISO 8601 with `Z` suffix.
   - The `timestamp_utc` column name is currently a lie — after this fix it becomes accurate. No rename needed (the name matches the new semantics).
   - Update all parser tests with hardcoded local Eastern output to assert the new UTC values.
2. **Display-layer rendering in user's preferred timezone**
   - Add a display helper that renders UTC in the user's preferred timezone, defaulting to America/New_York for the US market-hours convention most active users want.
   - Trade detail chart consumes this helper for candle x-axis and entry/exit marker positioning.

**Downstream audit pass (the real work)**

Audit every consumer that reads `.time` or `timestamp_utc` directly:
- `electron/stats/dashboard.ts`, `electron/analytics/get.ts`, `electron/calendar/get.ts`, `electron/calendar/weekly.ts`, `electron/reports/get.ts`, `src/core/insights/rules.ts`
- Any code that compares against market-hour windows (9:30, 11:00, etc.) must convert UTC → Eastern before comparing
- Spot-check every chart, list, and metric for timezone correctness after the audit

**Acceptance criteria**

- All six parsers store ISO 8601 UTC with `Z` suffix; verified by parser test updates.
- Trade detail chart for a known DAS trade renders entry/exit markers at the same x-position as the trader's source chart (Chartswatcher, TradingView, broker platform).
- Daily P&L bucketing still produces one point per Eastern trading day (not per UTC day).
- Edge Insights "best day of the week" still reports Monday/Tuesday/etc. in Eastern, not UTC-shifted.
- Existing parser tests pass after their hardcoded values update.

**Estimated effort:** 1-2 days. Touches every parser, every parser test, and downstream consumers reading time fields. Slots after Day 8 (number formatting + light/dark audit) and before Day 9 (error messages + in-app guide) so the timezone foundation is solid before user-facing polish work.

**Closes:** the v0.3.0 parking-lot entry "Store true UTC timestamps + rename column + add display-local rendering". Also retroactively closes the "Chart-window coverage for pre-market fills" entry filed this morning — that entry diagnosed the wrong root cause (data window) when the real issue was timestamp interpretation.


\### Day 9 — Error messages + in-app guide

\- Improve all import error messages

\- Build broker export guide modal

\- Step-by-step screenshots for DAS and Webull

\- "Request my broker" feedback link



\### Day 10 — Polish + edge case hunting

\- Re-import all real user fixtures end-to-end

\- Try weird cases: 1000+ trade files, encoding issues, BOM, etc.

\- Fix anything that breaks



\### Day 11 — Documentation + migration

\- Migration script for v0.1.6 → v0.2.0 data

\- Backwards compatibility verified

\- Update README, in-app help
  - Resolved 2026-05-25 (Day 11 afternoon, after investigation):
    Modal copy is clean on its own terms; no surgical fixes needed.
    Investigation surfaced cross-source vocabulary drift (modal,
    README, display-name table, parser code) for DAS broker format
    names — bigger than the bullet's scope. Full investigation
    handoff filed as v0.3.0 ticket under Architecture / Infrastructure.

\- Privacy review of test fixtures
  - Resolved 2026-05-25 (Day 11 morning, after explicit decision):
    test fixtures are local-only forever. Real beta-tester data
    (Tester B, Tester A, Webull user) never enters git. Existing
    .gitignore exclusion of test-fixtures/ stays as-is. README
    will document the policy publicly (see Day 11 README bullet).
    Future CI that needs reproducible import tests will require
    synthetic fixtures generated separately — filed for v0.3.0+.

\- Version bump to 0.2.0



\### Day 12 — Ship

\- Final smoke test

\- npm run package:win

\- Verify installer artifacts

\- GitHub Release draft + publish

\- Test auto-updater v0.1.6 → v0.2.0

\- Reply to Tester B, Tester A, Webull user with release notice



\---



\## Test fixtures



Real user data, stored locally only (not in git until permission granted):



\- test-fixtures/dtsm-tester-b-execution.csv (DAS execution format)

\- test-fixtures/dtsm-tester-b-account-report.csv (DAS fee data, when received)

\- test-fixtures/dtsm-tester-a-execution.csv (when received)

\- test-fixtures/webull-user-mobile.csv (Webull mobile format)

\- test-fixtures/webull-user-desktop.xlsx (Webull desktop format)

\- test-fixtures/lao-summary.csv (own DAS summary, current format)



Strip identifying info before committing to public repo:

\- Account names (real → ACCOUNT\_A)

\- User IDs (replace with synthetic IDs)

\- Cloid hashes if they reveal personal info

\- Anything else identifying



\---



\## Success criteria



v0.2.0 ships successfully when:



1\. All 5 parser formats import correctly against real user fixtures

2\. Round trip P\&L matches what users see in their broker platforms

3\. Fees are accurate when Account Report is provided

4\. Float and country auto-populate for known tickers

5\. No "format not recognized" errors for supported formats

6\. All v0.1.6 features still work

7\. Reports Daily P\&L matches sum of round trips

8\. Light mode and dark mode both render correctly

9\. Auto-updater test from v0.1.6 succeeds

10\. Tester B, Tester A, and Webull user can all import their files



If any of these fail at the smoke test, the release is blocked until fixed.



\---



\## Release-day action items



Items that MUST happen the moment v0.2.0 ships. Living checklist —
remove items as they complete on the day; leave the section in place
for the eventual v0.3.0 release to mirror.



\### Release-notes blurb — copy LOCKED (do not reword without review)



Paste this verbatim into the v0.2.0 GitHub release notes under the
"Fixed" section. Wording was reviewed and approved 2026-05-16.



&gt; \*\*Fixed:\*\* negative ECN rebates were not being credited correctly
&gt; in v0.1.6. If you had ECN rebates in earlier imports, re-drop the
&gt; affected daily-summary or Account Report CSVs and FugaEdge will
&gt; recompute trip P\&L with the corrected fees.



\### Release-notes paragraph — backup file location (copy LOCKED do not reword without review)



Paste this verbatim into the v0.2.0 GitHub release notes under
the "For users upgrading from v0.1.6" section. Wording reviewed
and approved 2026-05-25.



&gt; \*\*Upgrade safety:\*\* v0.2.0 creates two automatic safety backups
&gt; during the v0.1.6 → v0.2.0 upgrade, both saved in the FugaEdge
&gt; backups folder:
&gt;
&gt; - `fugaedge.db.pre-v0.2.0-<timestamp>.bak` — a snapshot of your
&gt;   journal in its original v0.1.6 shape, captured before any
&gt;   v0.2.0 changes. \*\*Restore from this file if you need to roll
&gt;   back to v0.1.6.\*\*
&gt; - `fugaedge.db.pre-v0.2.0-tz-<timestamp>.bak` — a snapshot
&gt;   captured during the upgrade, just before timestamps were
&gt;   converted to UTC. Mainly relevant for advanced troubleshooting;
&gt;   this file represents a mid-upgrade snapshot, not a clean
&gt;   restore point.
&gt;
&gt; Both files are safe to leave in place. If you need disk space
&gt; back, the second (`-tz-`) is the one you can delete first.



\### Tester B re-import outreach (Circle DM)



Tied to the release, not before. Tester B confirmed ECN rebates in his
data during 2026-05-15 dual-file testing — his v0.1.6 imports
under-report net P\&L by the rebate amount. Once v0.2.0 ships:



\- Send Circle DM letting him know v0.2.0 fixes the rebate handling

\- Suggest re-dropping his daily-summary / Account Report CSVs

\- Confirm via the new banner / Trade Detail Modal that the rebates
&#x20;  now show as negative fee values on affected trips



Do NOT bundle this with the Account Report fixture request (sent
separately, 2026-05-16, cold ask) — they're different asks and
bundling makes both easier to ignore.



\---



\## After v0.2.0 ships



Next conversations to have:

\- Formal DTSM beta cohort launch (with Tester B)

\- Tester B partnership terms revisited

\- v0.3.0 planning (foundation: shadcn, timezone, i18n, Mac)

\- Webull-specific marketing angle if that user base proves real

\- README update with supported broker matrix

